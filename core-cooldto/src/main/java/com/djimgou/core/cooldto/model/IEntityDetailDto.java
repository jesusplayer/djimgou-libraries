package com.djimgou.core.cooldto.model;

/**
 * Interface qui définit un détail du DTO dans l'application
 * Beaucoup plus lorsqu'on fait des requetes get
 * Tous les Dto de création/modification doivent l'implémenter pour faciliter l'extensibilité
 */
public interface IEntityDetailDto extends IDtoOfEntity {
}
